# hr-stay-competitive

Version: `0.1.0`

Use this skill for the single-file `技能升级` workflow in `D:\BaiduSyncdisk\个人PPT\Stay competitive`.

Current workflow contract:

- `09:20` generate silently
- `09:40` deduplicate silently
- `10:00` remind with the 6 questions only at strict retry timestamps
- `16:00` remind only when answers are still incomplete, otherwise write analysis back into the main file
